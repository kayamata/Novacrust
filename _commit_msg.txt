fix: clear stale auth user from localStorage so new name appears

The auth provider persisted the old "Leonard Soempit" user under
STORAGE_KEYS.auth (separate from the app store). The DATA_VERSION check
was only in the app store provider, so the stale user remained.

- Added DATA_VERSION check to AuthProvider hydration: clears
  STORAGE_KEYS.auth when the version changes so the updated
  DEMO_USER is used on the next sign-in
- Bumped DATA_VERSION to v4
- Demo user name is now "Jordan Carter" everywhere

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
