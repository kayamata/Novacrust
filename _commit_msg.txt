fix: force localStorage re-seed so GBP account appears

The app hydrates assets from localStorage on mount. Previous sessions
persisted the old INITIAL_ASSETS (without GBP), so the GBP card never
appeared even though the code was correct.

- Added DATA_VERSION constant ("v2") and nc_data_version storage key
- On hydration, if the stored version doesn't match, all mock data is
  re-seeded from defaults (INITIAL_ASSETS, DEMO_USER, etc.)
- resetToDefaults also saves the current data version
- This ensures the GBP account, updated user name, and reordered assets
  appear for existing users with stale localStorage

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com
