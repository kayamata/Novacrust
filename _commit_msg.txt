feat: replace dashboard card with swipeable balance carousel

Remove the virtual card widget from the Home/Dashboard (it remains on
the dedicated Cards page). Transform the balance section into a
horizontal carousel that cycles through all existing asset balances.

- New BalanceCarousel component with CSS scroll-snap for natural
  touch swiping on mobile and mouse/trackpad drag on desktop
- Each slide shows currency code, name, symbol/glyph, balance
  (animated), USD equivalent, and a subtle trend line
- Pagination dots underneath with smooth active-state animation
- Left/right arrow controls on desktop (sm+ screens)
- Action buttons (Add money, Send, Convert) on each slide navigate
  with ?currency= query param so the target page pre-selects that
  currency
- Send, Deposit, and Exchange pages now read useSearchParams to
  pre-select the currency from the carousel action
- Route files wrapped in Suspense for useSearchParams compatibility
- Layout rebalanced: greeting → carousel → quick actions →
  balances + recent activity
- All semantic design tokens preserved for light/dark mode

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
